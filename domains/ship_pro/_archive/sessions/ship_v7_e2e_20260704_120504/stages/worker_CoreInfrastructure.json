{
  "worker_role": "CoreInfrastructure",
  "wp_id_prefix": "CORE",
  "work_packages": [
    {
      "id": "CORE-001",
      "title": "Blackboard Manager with JSON Serialization and Atomic Writes",
      "description": "Implement the BlackboardManager class providing centralized state management with JSON unified serialization (D2 decision), atomic rename for crash-safe writes, and SHA-256 integrity verification. The atomic write pattern uses tempfile + fsync + os.replace to ensure either complete write success or preservation of previous state. Each stage state is stored as a JSON file with embedded SHA-256 checksum for integrity verification. The manager provides thread-safe per-stage locking via context managers for concurrent access control.",
      "acceptance_criteria": [
        "BlackboardManager class implements read_stage() and write_stage() methods with JSON serialization",
        "Atomic write pattern uses tempfile.mkstemp() + os.fsync() + os.replace() for crash-safe writes",
        "SHA-256 checksum is calculated and embedded in JSON data for integrity verification",
        "read_stage() verifies checksum on read and raises ValueError on mismatch",
        "Per-stage locking via stage_lock() context manager ensures thread-safe access",
        "update_stage() method provides atomic read-modify-write with locking",
        "list_stages() returns all available stage IDs",
        "delete_stage() removes stage state files",
        "Handles large data structures (>1MB) without performance degradation",
        "No temporary files left after successful or failed writes"
      ],
      "dependencies": [],
      "estimated_effort": "16 hours",
      "deliverables": [
        "ship_framework/blackboard.py - BlackboardManager class implementation",
        "tests/test_blackboard.py - 13 unit tests for blackboard operations",
        "tests/test_crash_recovery.py - 16 crash recovery scenario tests"
      ]
    },
    {
      "id": "CORE-002",
      "title": "Per-Stage Lock Manager with Task ID Enforcement",
      "description": "Implement the StageLockManager class providing distributed locking mechanism with unique task ID generation and verification. Uses threading.Condition for efficient lock acquisition with configurable timeout and exponential backoff. Each lock acquisition records task_id, stage_id, acquisition timestamp, and owner thread. The manager enforces task ownership - only the task that acquired a lock can release it. Supports automatic lock expiration based on timeout, stale lock detection, and force release for administrative operations. Provides context manager interface for convenient lock usage.",
      "acceptance_criteria": [
        "StageLockManager class implements acquire_lock() and release_lock() methods",
        "generate_task_id() creates unique task identifiers with 'task_' prefix",
        "Lock acquisition supports configurable timeout with exponential backoff",
        "LockOwnershipError raised when non-owner attempts to release lock",
        "LockAcquisitionError raised when timeout expires during acquisition",
        "is_locked() checks if stage is currently locked",
        "get_lock_owner() returns task_id of lock holder",
        "get_locked_stages() returns set of all locked stage IDs",
        "lock_stage() context manager provides convenient lock acquisition/release",
        "Automatic lock expiration based on timeout parameter",
        "force_release() allows administrative lock release without ownership check",
        "Thread-safe operations using threading.Condition and threading.RLock"
      ],
      "dependencies": [],
      "estimated_effort": "12 hours",
      "deliverables": [
        "ship_framework/lock_manager.py - StageLockManager class implementation",
        "tests/test_lock_manager.py - 17 unit tests for lock operations and contention"
      ]
    },
    {
      "id": "CORE-003",
      "title": "Python Skeleton Framework with Loop Engine Integration",
      "description": "Implement the core framework structure with LoopEngine class providing entry points for Loop engine integration. The engine coordinates loop creation, stage execution, and task management using BlackboardManager and StageLockManager. Supports hierarchical loop structure (Project/Domain/Phase levels) with parent-child relationships. Stage execution is wrapped with lock acquisition, task tracking, and automatic state updates. The engine provides methods for creating loops, adding stages, executing stages with custom handlers, completing/failing loops, and verifying integrity. Includes data models for StageState, LoopState, and TaskRecord with serialization support.",
      "acceptance_criteria": [
        "LoopEngine class provides create_loop() and get_loop() methods",
        "add_stage_to_loop() creates stages with unique IDs and context",
        "execute_stage() wraps handler execution with lock acquisition and task tracking",
        "Handler functions receive StageState and return result dictionaries",
        "Task records track status (PENDING/RUNNING/COMPLETED/FAILED), timestamps, and results",
        "complete_loop() and fail_loop() update loop state with results/errors",
        "get_stage_state() retrieves current stage state from blackboard",
        "verify_integrity() checks stage state file integrity using embedded checksum",
        "list_active_loops() returns all active loop IDs",
        "cleanup_completed() removes loop and stage state files",
        "Supports concurrent execution of different stages",
        "Loop hierarchy tracking with parent_loop_id relationships"
      ],
      "dependencies": [
        "CORE-001",
        "CORE-002"
      ],
      "estimated_effort": "14 hours",
      "deliverables": [
        "ship_framework/engine.py - LoopEngine class implementation",
        "ship_framework/models.py - StageState, LoopState, TaskRecord data models",
        "tests/test_engine.py - 21 unit tests for engine operations",
        "tests/test_models.py - 27 unit tests for data models"
      ]
    },
    {
      "id": "CORE-004",
      "title": "SHA-256 Integrity Verification System",
      "description": "Implement the IntegrityVerifier class providing comprehensive SHA-256 based integrity verification for state files. Supports checksum calculation for bytes, files, and JSON data with configurable key exclusion. Provides methods for saving/loading checksums to separate .sha256 files, verifying file integrity, and batch verification of directories. The verifier detects corruption, tampering, and incomplete writes by comparing calculated checksums against stored values. Returns IntegrityResult objects with detailed verification status including expected/actual checksums and error messages.",
      "acceptance_criteria": [
        "IntegrityVerifier class implements calculate_checksum() for bytes data",
        "calculate_file_checksum() reads file and calculates SHA-256 hash",
        "calculate_json_checksum() serializes JSON with sort_keys=True for consistency",
        "exclude_keys parameter allows excluding fields from checksum calculation",
        "save_checksum() writes checksum to separate .sha256 file",
        "load_checksum() reads stored checksum from .sha256 file",
        "verify_file() compares file checksum against stored value",
        "verify_json() verifies JSON file with key exclusion support",
        "verify_directory() performs batch verification on all matching files",
        "IntegrityResult provides is_valid property and detailed error information",
        "Detects file corruption, unauthorized modification, and missing checksums",
        "delete_checksum() removes stored checksum files"
      ],
      "dependencies": [],
      "estimated_effort": "10 hours",
      "deliverables": [
        "ship_framework/integrity.py - IntegrityVerifier class implementation",
        "tests/test_integrity.py - 21 unit tests for integrity verification"
      ]
    }
  ],
  "metadata": {
    "total_effort_hours": 52,
    "test_coverage": {
      "total_tests": 115,
      "test_files": [
        "test_blackboard.py (13 tests)",
        "test_lock_manager.py (17 tests)",
        "test_engine.py (21 tests)",
        "test_models.py (27 tests)",
        "test_integrity.py (21 tests)",
        "test_crash_recovery.py (16 tests)"
      ],
      "all_tests_passing": true
    },
    "architecture_decisions": [
      "D2: JSON unified serialization for human readability and cross-language compatibility",
      "Atomic write pattern: tempfile + fsync + os.replace for crash safety",
      "Embedded SHA-256 checksum in JSON for integrity verification",
      "Per-stage locking to minimize contention between independent stages",
      "Task ID enforcement to prevent unauthorized lock release",
      "Hierarchical loop structure (Project/Domain/Phase) for fractal architecture"
    ],
    "key_features": [
      "Crash-safe atomic writes with no partial state visibility",
      "SHA-256 integrity verification detects corruption and tampering",
      "Thread-safe concurrent access with per-stage locking",
      "Task ID ownership enforcement prevents race conditions",
      "Configurable lock timeout with exponential backoff",
      "Automatic lock expiration and stale lock detection",
      "Comprehensive test coverage including crash recovery scenarios"
    ],
    "design_patterns": [
      "Context Manager Pattern for lock acquisition/release",
      "Atomic Write Pattern for crash-safe file operations",
      "Checksum Pattern for integrity verification",
      "Factory Pattern for task ID generation",
      "Strategy Pattern for stage execution handlers"
    ],
    "dependencies": {
      "python": ">=3.10",
      "external": "None (pure Python standard library)",
      "testing": [
        "pytest>=7.0.0",
        "pytest-cov>=4.0.0"
      ]
    },
    "file_structure": {
      "ship_framework/": [
        "__init__.py",
        "blackboard.py (6.8 KB)",
        "lock_manager.py (8.8 KB)",
        "integrity.py (9.3 KB)",
        "models.py (9.0 KB)",
        "engine.py (9.6 KB)"
      ],
      "tests/": [
        "__init__.py",
        "test_blackboard.py (9.6 KB)",
        "test_lock_manager.py (12.5 KB)",
        "test_integrity.py (10.8 KB)",
        "test_crash_recovery.py (12.6 KB)",
        "test_engine.py (12.8 KB)",
        "test_models.py (12.2 KB)"
      ],
      "documentation": [
        "README.md (5.9 KB)"
      ],
      "configuration": [
        "pyproject.toml (877 B)",
        "requirements.txt (32 B)"
      ]
    }
  },
  "optional_suggestions": [
    "Consider adding distributed locking support (e.g., Redis-based) for multi-process deployments",
    "Add metrics collection for lock contention rates and acquisition times",
    "Implement automatic retry logic for transient integrity verification failures",
    "Add support for encrypted state files using AES-256",
    "Consider implementing write-ahead logging (WAL) for additional crash recovery guarantees",
    "Add support for state file versioning and rollback capabilities",
    "Implement background integrity verification for large state files",
    "Add support for custom serialization formats (e.g., MessagePack, Protocol Buffers)"
  ]
}