{
  "worker_role": "QualityGate",
  "wp_id_prefix": "QG",
  "work_packages": [
    {
      "id": "QG-001",
      "title": "PydanticValidator — Layer 1 确定性验证",
      "description": "实现 Quality Gate 的第一层：基于 Pydantic 的确定性验证器。负责对阶段交付物进行快速格式校验，包括字段存在性检查、类型匹配、依赖关系无环检测（acyclic dependency validation）。作为 fast filter，在毫秒级完成验证，过滤掉明显的格式错误和结构缺陷，避免将无效输入传递给 LLM Judge 层浪费 token。PydanticValidator 接收 stage_output（JSON 格式）和对应的 schema 定义，输出 PASS/FAIL 及详细错误列表。",
      "acceptance_criteria": [
        "AC1: PydanticValidator 类接受 Pydantic model 或 JSON Schema 作为验证规则输入，返回结构化验证结果（PASS/FAIL + errors 列表）",
        "AC2: 字段存在性验证 — 缺失必填字段时返回 FAIL 并列出所有缺失字段名",
        "AC3: 类型检查 — 字段类型不匹配时返回 FAIL 并标注期望类型与实际类型",
        "AC4: 无环依赖检测 — 对 dependencies 字段执行拓扑排序，检测到环时返回 FAIL 并报告环路径",
        "AC5: 验证耗时 < 50ms（对 100 字段以内的 JSON 对象）",
        "AC6: 验证结果为 FAIL 时，errors 列表包含每个错误的 field_path、expected、actual 三个字段"
      ],
      "dependencies": [],
      "estimated_effort": "8h",
      "deliverables": [
        "quality_gate/pydantic_validator.py — PydanticValidator 类实现",
        "quality_gate/schemas.py — 各阶段输出的 Pydantic model 定义（StageOutput base + 各阶段子类）",
        "tests/unit/test_pydantic_validator.py — 覆盖字段缺失、类型错误、无环检测、正常通过四类场景"
      ]
    },
    {
      "id": "QG-002",
      "title": "LLMJudge — Layer 2 语义评估（严格隔离）",
      "description": "实现 Quality Gate 的第二层：LLM-as-Judge 语义评估器。遵循 D10 约束（同模型族 Judge + 严格隔离），使用与 Executor 相同模型族但独立 prompt 的 Judge 视角，评估交付物的架构合理性、原则对齐度、质量和可行性。LLMJudge 接收 stage_output 和原始 stage_goal，通过结构化 prompt 生成四维评分（architecture_soundness, principle_alignment, quality, feasibility）及理由。严格隔离意味着 Judge prompt 不包含 Executor 的内部推理过程，仅基于交付物本身进行独立评估。",
      "acceptance_criteria": [
        "AC1: LLMJudge 类接受 stage_output（JSON）和 stage_goal（str）作为输入，返回四维评分 + 综合判定 + 理由",
        "AC2: 四维评分 — architecture_soundness / principle_alignment / quality / feasibility，每项 1-5 分",
        "AC3: 综合判定逻辑 — 四项均 >= 3 则 PASS；任一项 < 3 但综合加权 >= 3.0 则 CONDITIONAL；加权 < 3.0 则 FAIL",
        "AC4: 严格隔离 — Judge prompt 中不包含 Executor 的 CoT / 内部推理 / 中间状态，仅包含 stage_goal + stage_output + 评估标准",
        "AC5: 同模型族约束 — LLMJudge 的 model 参数必须与 Executor 使用同一模型族（通过 config.model_family 校验），不同族时抛出 ModelIsolationError",
        "AC6: Judge prompt 使用 Role + Context + Constraints + Examples + Output Schema 结构（符合 AGENTS.md 4.1）",
        "AC7: 支持 structured output 返回（JSON mode），确保评分结果可解析"
      ],
      "dependencies": [],
      "estimated_effort": "12h",
      "deliverables": [
        "quality_gate/llm_judge.py — LLMJudge 类实现（含 prompt 模板、评分逻辑、模型族校验）",
        "quality_gate/prompts/judge_eval.py — Judge 评估 prompt 模板（四维评分 + 结构化输出 schema）",
        "tests/unit/test_llm_judge.py — 覆盖 PASS/CONDITIONAL/FAIL 判定、模型族隔离校验、prompt 结构验证"
      ]
    },
    {
      "id": "QG-003",
      "title": "DecisionMerger — Layer 3 合并决策",
      "description": "实现 Quality Gate 的第三层：合并决策器。接收 Layer 1（PydanticValidator）和 Layer 2（LLMJudge）的结果，按照预定义规则合并为最终决策：PASS / CONDITIONAL / FAIL。合并规则：L1 FAIL → 直接 FAIL（不进入 L2 节省 token）；L1 PASS + L2 FAIL → CONDITIONAL（格式正确但语义有问题，需人工或重试）；L1 PASS + L2 CONDITIONAL → CONDITIONAL；L1 PASS + L2 PASS → PASS。CONDITIONAL 状态携带具体改进建议（来自 L2 Judge 的 feedback），用于反馈给上游 stage 重试。",
      "acceptance_criteria": [
        "AC1: DecisionMerger 接受 l1_result（PydanticValidator 输出）和 l2_result（LLMJudge 输出），返回最终决策",
        "AC2: 合并规则 — L1 FAIL → FAIL（短路，不调用 L2）；L1 PASS + L2 PASS → PASS；L1 PASS + L2 CONDITIONAL → CONDITIONAL；L1 PASS + L2 FAIL → CONDITIONAL",
        "AC3: CONDITIONAL 输出必须包含 improvement_suggestions 列表（来自 L2 feedback），供上游重试使用",
        "AC4: FAIL 输出必须包含 failure_reason 和 failed_layer（L1 或 L2）标识",
        "AC5: 输出结构化结果对象，包含 decision / confidence / details / improvement_suggestions 字段"
      ],
      "dependencies": [
        "QG-001",
        "QG-002"
      ],
      "estimated_effort": "6h",
      "deliverables": [
        "quality_gate/decision_merger.py — DecisionMerger 类实现",
        "tests/unit/test_decision_merger.py — 覆盖全部合并规则路径 + CONDITIONAL 建议生成"
      ]
    },
    {
      "id": "QG-004",
      "title": "QualityGate 主类 — 三层流水线编排 + UTD 断路器",
      "description": "实现 QualityGate 主类，编排三层验证流水线（L1 → L2 → L3 合并）并集成 UTD（Update-Then-Downgrade）断路器策略。QualityGate 作为阶段转换的唯一守门人，每个 stage 完成后调用 quality_gate.evaluate(stage_output, stage_goal) 获取最终决策。UTD 策略：当决策为 CONDITIONAL/FAIL 时，允许上游更新交付物后重新提交验证，最多 3 轮；若 3 轮后仍未 PASS，则触发断路器，降级为人工介入或跳过该阶段并记录风险。断路器监控连续 reject 率，当最近 10 次评估中 reject 率 > 50% 时触发冷却期（暂停自动重试，等待人工确认）。RTO 4.5min（R1 约束）。",
      "acceptance_criteria": [
        "AC1: QualityGate.evaluate(stage_output, stage_goal) 返回最终决策（PASS/CONDITIONAL/FAIL）+ 完整审计记录",
        "AC2: 三层流水线 — L1 失败短路（不调 L2）；L1 通过 → 调 L2 → L3 合并",
        "AC3: UTD 策略 — CONDITIONAL/FAIL 时返回 improvement_suggestions 供上游重试，max_retries=3",
        "AC4: 3 轮重试后仍未 PASS → 断路器触发，返回 FAIL + circuit_broken=True + 降级建议",
        "AC5: 断路器状态 — 滑动窗口记录最近 10 次评估结果，reject 率 > 50% 时进入冷却期（cooldown_seconds 可配置，默认 300s）",
        "AC6: 冷却期内所有 evaluate 调用直接返回 CONDITIONAL + circuit_breaker_active=True，不执行 L1/L2",
        "AC7: 整体 evaluate 耗时 < 4.5min（R1 RTO 约束），超时返回 CONDITIONAL + timeout=True",
        "AC8: 每次 evaluate 生成 audit_entry（timestamp, stage_name, l1_result, l2_result, final_decision, retry_round, duration_ms），支持审计日志链"
      ],
      "dependencies": [
        "QG-001",
        "QG-002",
        "QG-003"
      ],
      "estimated_effort": "14h",
      "deliverables": [
        "quality_gate/quality_gate.py — QualityGate 主类（三层编排 + UTD 断路器 + 审计记录）",
        "quality_gate/circuit_breaker.py — CircuitBreaker 类（滑动窗口 + 冷却期 + 状态机）",
        "quality_gate/config.py — QualityGateConfig（max_retries, cooldown_seconds, rto_timeout, model_family 等可配置参数）",
        "tests/unit/test_quality_gate.py — 覆盖三层流水线全路径 + UTD 重试 + 断路器触发/冷却/恢复",
        "tests/integration/test_quality_gate_integration.py — 端到端集成测试：正常 PASS 流、L1 失败短路、L2 CONDITIONAL → 重试 → PASS、3 轮失败 → 断路器、reject 率触发冷却期"
      ]
    }
  ],
  "metadata": {
    "design_rationale": {
      "layer_separation": "三层分离确保关注点隔离：L1 纯代码（确定性、毫秒级）、L2 纯语义（LLM 独立视角）、L3 纯决策（规则合并）。每层可独立测试和替换。",
      "l1_short_circuit": "L1 失败不调 L2 是关键的 token 节省策略 — 格式错误的输入不值得浪费 LLM 评估。",
      "utd_over_pure_retry": "UTD（Update-Then-Downgrade）而非纯重试：每轮重试携带 L2 的改进建议，上游可针对性修改，而非盲目重试。3 轮后降级避免无限循环。",
      "circuit_breaker": "滑动窗口 reject 率监控防止系统性问题（如 schema 变更导致批量失败）浪费资源。冷却期强制人工介入。",
      "d10_compliance": "LLMJudge 严格遵循 D10 约束 — 同模型族 + 独立 prompt + 不暴露 Executor 内部状态。Judge 只看交付物，不看推理过程。",
      "effort_distribution": "总 40h。L2（LLMJudge）最重（12h）因 prompt 工程 + 模型族隔离 + 结构化输出最复杂；QG-004（14h）因编排逻辑 + 断路器 + 审计最复杂。"
    },
    "constraint_traceability": {
      "D10": "QG-002 AC4/AC5 — 同模型族 Judge + 严格隔离",
      "R1": "QG-004 AC7 — UTD + max 3 轮 + 断路器，RTO 4.5min",
      "AGENTS_4.3": "QG-001/002/003/004 — 三层 Gate 架构（确定性过滤 → LLM 语义 → 合并决策）"
    },
    "total_estimated_effort_hours": 40
  },
  "optional_suggestions": [
    "建议为 LLMJudge 的 prompt 模板建立版本管理机制，prompt 变更需记录版本号，便于回溯 Judge 标准变化对评估结果的影响。",
    "考虑为 CircuitBreaker 增加 metrics 导出（Prometheus 格式），与可观测性组件（Phase 3 交付物）集成，实现断路器状态的实时监控和告警。",
    "CONDITIONAL 降级路径可考虑支持 'auto-fix' 模式：对 L1 格式错误（如字段名拼写），由 QualityGate 自动修正后重新验证，减少不必要的 L2 调用。"
  ]
}