{
  "version": "1.0",
  "generated_at": "2026-06-22T20:04:00Z",
  "description": "OpenClaw Diagnostics Field Mapping Table for DeepFlow",
  "mappings": [
    {
      "field_name": "tokens",
      "description": "Token usage for LLM calls",
      "source_paths": [
        "diagnostics.tokens.input_tokens",
        "diagnostics.tokens.output_tokens",
        "metrics.token_usage.input",
        "metrics.token_usage.output"
      ],
      "target_type": "integer",
      "required": true,
      "fallback": "从阶段文件时间戳计算",
      "fallback_description": "当Direct diagnostics不可用时，使用阶段开始/结束时间差估算token消耗"
    },
    {
      "field_name": "input_tokens",
      "description": "Input token count",
      "source_paths": [
        "diagnostics.tokens.input_tokens",
        "gen_ai.usage.input_tokens",
        "metrics.input_tokens"
      ],
      "target_type": "integer",
      "required": true,
      "fallback": "None"
    },
    {
      "field_name": "output_tokens",
      "description": "Output token count",
      "source_paths": [
        "diagnostics.tokens.output_tokens",
        "gen_ai.usage.output_tokens",
        "metrics.output_tokens"
      ],
      "target_type": "integer",
      "required": true,
      "fallback": "None"
    },
    {
      "field_name": "cost",
      "description": "Total cost in USD",
      "source_paths": [
        "diagnostics.cost",
        "metrics.cost_usd",
        "gen_ai.usage.cost",
        " pricing.total_cost"
      ],
      "target_type": "number",
      "required": true,
      "fallback": "从tokens推断 (input_tokens * input_price + output_tokens * output_price)"
    },
    {
      "field_name": "duration",
      "description": "Execution duration in seconds",
      "source_paths": [
        "diagnostics.duration",
        "metrics.duration_seconds",
        " gen_ai.usage.duration_seconds"
      ],
      "target_type": "number",
      "required": true,
      "fallback": "从阶段文件时间戳提取 (粗粒度，误差 < 10%)"
    },
    {
      "field_name": "worker_id",
      "description": "Worker identifier",
      "source_paths": [
        "diagnostics.worker_id",
        "worker.id",
        "agent_id"
      ],
      "target_type": "string",
      "required": true,
      "fallback": "从阶段文件名推断 (planning.json → worker='planner')"
    },
    {
      "field_name": "phase_id",
      "description": "Phase identifier",
      "source_paths": [
        "diagnostics.phase_id",
        "phase.id",
        "stage_id"
      ],
      "target_type": "string",
      "required": true,
      "fallback": "从阶段文件名推断 (planning.json → phase='planning')"
    },
    {
      "field_name": "run_id",
      "description": "Run/session identifier",
      "source_paths": [
        "diagnostics.run_id",
        "session_id",
        "run_uuid",
        " uuid"
      ],
      "target_type": "string",
      "required": true,
      "fallback": "从Blackboard会话目录名推断"
    },
    {
      "field_name": "model",
      "description": "Model identifier",
      "source_paths": [
        "diagnostics.model",
        "gen_ai.model",
        " llm_model"
      ],
      "target_type": "string",
      "required": false,
      "fallback": "None"
    },
    {
      "field_name": "timestamp",
      "description": "Event timestamp",
      "source_paths": [
        "diagnostics.timestamp",
        " gen_ai.usage.timestamp",
        "metrics.time"
      ],
      "target_type": "string",
      "required": true,
      "fallback": "从阶段文件mtime获取"
    },
    {
      "field_name": "tool_call_count",
      "description": "Number of tool calls made",
      "source_paths": [
        "diagnostics.tool_call_count",
        "gen_ai.usage.tool_calls",
        "metrics.tool_calls"
      ],
      "target_type": "integer",
      "required": false,
      "fallback": "从stage阶段持续时间推断"
    },
    {
      "field_name": "event_type",
      "description": "Event type for the diagnostic record",
      "source_paths": [
        "diagnostics.event_type",
        "event.type"
      ],
      "target_type": "string",
      "required": true,
      "fallback": "从阶段文件名推断 (planning.json → event_type='phase_end')"
    },
    {
      "field_name": "worker_name",
      "description": "Worker name (human-readable)",
      "source_paths": [
        "diagnostics.worker_name",
        "worker.name",
        "agent_name"
      ],
      "target_type": "string",
      "required": false,
      "fallback": "从阶段文件名推断 (planning.json → worker_name='Planner')"
    },
    {
      "field_name": "phase_name",
      "description": "Phase name (human-readable)",
      "source_paths": [
        "diagnostics.phase_name",
        "phase.name",
        "stage_name"
      ],
      "target_type": "string",
      "required": false,
      "fallback": "从阶段文件名推断 (planning.json → phase_name='Planning')"
    }
  ],
  "fallback_strategies": [
    {
      "strategy_id": "F-001",
      "name": "阶段文件名推断",
      "description": "当字段直接不可用时，从阶段文件名推断worker_id/phase_id",
      "pattern": "^(.*)\\.json$ → worker_name='$1', phase_name='$1', worker_id='worker_$1', phase_id='phase_$1'",
      "accuracy": "high",
      "examples": [
        "planning.json → worker='planner', phase='planning'",
        "review_technical.json → worker='reviewer', phase='technical_review'",
        "consolidator.json → worker='consolidator', phase='consolidation'"
      ]
    },
    {
      "strategy_id": "F-002",
      "name": "时间戳提取Duration",
      "description": "从阶段文件系统时间戳计算粗粒度duration",
      "formula": "duration = file_mtime - previous_file_mtime",
      "accuracy": "< 10% error (粗粒度)",
      "precedence": "当diagnostics.duration不可用时"
    },
    {
      "strategy_id": "F-003",
      "name": "黑板会话目录作为run_id",
      "description": "当run_id字段不可用时，使用Blackboard会话目录名",
      "pattern": "^.*?_(session|architecture)_([a-f0-9]+)$ → run_id='$2'",
      "accuracy": "high",
      "examples": [
        "DeepFlow_开发者可观测性系统_architecture_c92c1412 → run_id='c92c1412'",
        "DHR_技术方案_bc9e1234 → run_id='bc9e1234'"
      ]
    },
    {
      "strategy_id": "F-004",
      "name": "Tokens推断Cost",
      "description": "当cost字段不可用时，从tokens和模型价格推断",
      "formula": "cost = input_tokens * input_price_per_1k + output_tokens * output_price_per_1k",
      "accuracy": "depends on price accuracy",
      "default_prices": {
        "gpt-4o": {"input": 2.50, "output": 10.00},
        "gpt-4-turbo": {"input": 10.00, "output": 30.00},
        "claude-3-5-sonnet": {"input": 3.00, "output": 15.00}
      }
    }
  ],
  "validation_requirements": {
    "tokens": {
      "min_fields": ["input_tokens", "output_tokens"],
      "type": "integer",
      "required": true
    },
    "cost": {
      "min_fields": ["cost", "cost_usd"],
      "type": "number",
      "required": true
    },
    "duration": {
      "min_fields": ["duration", "duration_seconds"],
      "type": "number",
      "required": true
    },
    "worker_phase": {
      "min_fields": ["worker_id", "phase_id"],
      "type": "string",
      "required": true,
      "alternatives": ["agent_id", "stage_id"]
    },
    "run_id": {
      "min_fields": ["run_id", "session_id"],
      "type": "string",
      "required": true
    }
  }
}
