{{- if .Values.agent.logging.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "otel-collector.fullname" . }}-agent-logging
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "otel-collector.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
data:
  logging-config.yaml: |
    # Agent Collector Structured Logging Configuration
    # Uses JSON format for machine-parseable logs.
    # Authorization headers are redacted to prevent secret leakage.

    service:
      telemetry:
        logs:
          level: {{ .Values.agent.logging.level | default "info" }}
          encoding: json
          output_paths:
            - stdout
          error_output_paths:
            - stderr
          initial_fields:
            component: agent
            collector: otel-collector-agent
          {{- if .Values.agent.logging.sampling }}
          sampling:
            enabled: true
            initial: {{ .Values.agent.logging.sampling.initial | default 10 }}
            thereafter: {{ .Values.agent.logging.sampling.thereafter | default 100 }}
          {{- end }}

    processors:
      # Redact sensitive headers from logs before any processing
      attributes/redact-auth:
        actions:
          - key: http.request.header.authorization
            action: delete
          - key: authorization
            action: delete
          - key: Authorization
            action: delete

    service:
      pipelines:
        logs:
          processors:
            - attributes/redact-auth
            - memory_limiter
            - batch
{{- end }}