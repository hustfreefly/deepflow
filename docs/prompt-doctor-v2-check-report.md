# Prompt Doctor V2 检查报告

**检查时间**: 2026-07-29  
**检查范围**: Solution Pro 5 个核心 prompt  
**检查标准**: Prompt Doctor V2 稳健性优先版

---

## 检查结果汇总

| Prompt | Layer 1 | Layer 3 | Layer 2 | 通用 | 总体评级 |
|--------|---------|---------|---------|------|----------|
| meta_planner.md | ✅ 通过 | ✅ 通过 | ⚠️ 部分 | ✅ 通过 | **B+** |
| convergence_planner.md | ✅ 通过 | ✅ 通过 | ⚠️ 部分 | ✅ 通过 | **B+** |
| orchestrator.md | ✅ 通过 | ✅ 通过 | ⚠️ 部分 | ✅ 通过 | **B+** |
| planning_module.md | ✅ 通过 | ✅ 通过 | ⚠️ 部分 | ✅ 通过 | **B+** |
| research_module.md | ✅ 通过 | ✅ 通过 | ⚠️ 部分 | ✅ 通过 | **B+** |

**总体评级**: **B+**（所有 prompt 通过 Layer 1 和 Layer 3 必须检查）

---

## 详细检查结果

### 1. meta_planner.md

#### Layer 1 检查（必须通过）✅

- ✅ **任务边界**: 有明确声明（"你是 Solution Pro V3.3 的 Meta-Planner。你的任务是分析用户任务，决定需要哪些领域专家来规划方案。"）
- ✅ **状态转移表**: 隐含在契约笼子中（输入验证 → 处理 → 输出验证 → 错误处理）
- ✅ **完成条件**: 有明确声明（"输出必须通过 ExpertManifestSchema 校验"）
- ✅ **恢复机制**: 有明确定义（"错误处理契约（智能恢复，不降级）"）
- ✅ **错误分类**: 有明确定义（瞬时故障 / 可恢复错误 / 不可恢复错误）
- ✅ **中间产物持久化**: 有明确定义（"输出文件：stages/meta_planning.json"）
- ✅ **没有 Fail Fast 思维**: ✅ 已移除 Fail Fast，改为智能恢复

#### Layer 3 检查（必须通过）✅

- ✅ **输入源**: 有明确定义（"data/living_spec.json（优先）或 data/frozen_spec.json"）
- ✅ **输出目标**: 有明确定义（"stages/meta_planning.json"）
- ✅ **中间产物清单**: 有明确定义（输出文件路径）

#### Layer 2 检查（推荐通过）⚠️

- ⚠️ **质量预期**: 部分定义（"Gate A 权重和必须 = 1.0"、"P0 REQ 必须 100% 覆盖"）
- ✅ **验证标准**: 有明确定义（"输出必须通过 ExpertManifestSchema 校验"）
- ❌ **正面示例 + 负面示例**: 缺失

#### 通用检查 ✅

- ✅ **Prompt 结构清晰**: 分层 + 编号
- ✅ **关键约束放在顶部**: 契约笼子在顶部（前 500 token）
- ❌ **有负面示例**: 缺失
- ✅ **spawn task 使用文件引用**: 符合（spawn task 只传"去读 X 文件执行"）

**改进建议**:
- 添加正面示例 + 负面示例（Layer 2）
- 添加负面示例（通用检查）

---

### 2. convergence_planner.md

#### Layer 1 检查（必须通过）✅

- ✅ **任务边界**: 有明确声明（"你是 Solution Pro V3.3 的 Convergence Planner。你的任务是将多个 Expert Plan 合并为统一的约束集和验证清单。"）
- ✅ **状态转移表**: 隐含在契约笼子中（输入验证 → 处理 → 输出验证 → 错误处理）
- ✅ **完成条件**: 有明确声明（"输出必须包含所有必需字段"）
- ✅ **恢复机制**: 有明确定义（"错误处理契约（智能恢复，不降级）"）
- ✅ **错误分类**: 有明确定义（瞬时故障 / 可恢复错误 / 不可恢复错误）
- ✅ **中间产物持久化**: 有明确定义（"输出文件：stages/planning_convergence.json"）
- ✅ **没有 Fail Fast 思维**: ✅ 已移除 Fail Fast，改为智能恢复

#### Layer 3 检查（必须通过）✅

- ✅ **输入源**: 有明确定义（"data/living_spec.json"、"stages/meta_planning.json"、"stages/expert_plans/*.json"）
- ✅ **输出目标**: 有明确定义（"stages/planning_convergence.json"）
- ✅ **中间产物清单**: 有明确定义（输出文件路径）

#### Layer 2 检查（推荐通过）⚠️

- ⚠️ **质量预期**: 部分定义（"merge_ratio 必须在 0.5 - 0.8 范围内"）
- ✅ **验证标准**: 有明确定义（"输出必须包含所有必需字段"）
- ❌ **正面示例 + 负面示例**: 缺失

#### 通用检查 ✅

- ✅ **Prompt 结构清晰**: 分层 + 编号
- ✅ **关键约束放在顶部**: 契约笼子在顶部（前 500 token）
- ❌ **有负面示例**: 缺失
- ✅ **spawn task 使用文件引用**: 符合

**改进建议**:
- 添加正面示例 + 负面示例（Layer 2）
- 添加负面示例（通用检查）

---

### 3. orchestrator.md

#### Layer 1 检查（必须通过）✅

- ✅ **任务边界**: 有明确声明（"你是 Solution Pro V4.0 的薄层调度器（Orchestrator Agent，depth-1）。"）
- ✅ **状态转移表**: 有明确定义（"执行循环" + "状态机"）
- ✅ **完成条件**: 有明确声明（"PIPELINE_COMPLETED → 结束 turn"）
- ✅ **恢复机制**: 有明确定义（"错误处理契约（智能重试，不降级）"）
- ✅ **错误分类**: 有明确定义（瞬时故障 / 可恢复错误 / 不可恢复错误）
- ✅ **中间产物持久化**: 有明确定义（"模块输出契约"）
- ✅ **没有 Fail Fast 思维**: ✅ 已移除 Fail Fast，改为智能重试

#### Layer 3 检查（必须通过）✅

- ✅ **输入源**: 有明确定义（"模块输出"）
- ✅ **输出目标**: 有明确定义（".completed" 标记）
- ✅ **中间产物清单**: 有明确定义（"模块输出契约"）

#### Layer 2 检查（推荐通过）⚠️

- ⚠️ **质量预期**: 部分定义（"文件大小必须 >= 配置的最小值"）
- ✅ **验证标准**: 有明确定义（"文件内容必须是有效 JSON"）
- ❌ **正面示例 + 负面示例**: 缺失

#### 通用检查 ✅

- ✅ **Prompt 结构清晰**: 分层 + 编号
- ✅ **关键约束放在顶部**: 契约笼子在顶部（前 500 token）
- ❌ **有负面示例**: 缺失
- ✅ **spawn task 使用文件引用**: 符合

**改进建议**:
- 添加正面示例 + 负面示例（Layer 2）
- 添加负面示例（通用检查）

---

### 4. planning_module.md

#### Layer 1 检查（必须通过）✅

- ✅ **任务边界**: 有明确声明（"你是 Planning 模块的编排器 Agent。你的工作：1. 直接通过 sessions_spawn 创建 Workers"）
- ✅ **状态转移表**: 有明确定义（"Wake Response Protocol" + "执行流程"）
- ✅ **完成条件**: 有明确声明（"全部步骤完成 + 写入 .planning_completed 后才能结束 turn"）
- ✅ **恢复机制**: 有明确定义（"智能重试" + "失败报告格式"）
- ✅ **错误分类**: 有明确定义（瞬时故障 / 可恢复错误 / 不可恢复错误）
- ✅ **中间产物持久化**: 有明确定义（"stages/planning_convergence.json"）
- ✅ **没有 Fail Fast 思维**: ✅ 已移除 Fail Fast（4 处残留已修复）

#### Layer 3 检查（必须通过）✅

- ✅ **输入源**: 有明确定义（"data/frozen_spec.json"、"stages/meta_planning.json" 等）
- ✅ **输出目标**: 有明确定义（"stages/planning_convergence.json"）
- ✅ **中间产物清单**: 有明确定义（Worker Spawn 清单）

#### Layer 2 检查（推荐通过）⚠️

- ⚠️ **质量预期**: 部分定义（"验证 Worker 输出 — 确认每个 Worker 的输出已写入 Blackboard 并符合 Schema"）
- ✅ **验证标准**: 有明确定义（"Gate A/B 评分"）
- ❌ **正面示例 + 负面示例**: 缺失

#### 通用检查 ✅

- ✅ **Prompt 结构清晰**: 分层 + 编号
- ✅ **关键约束放在顶部**: 生存铁律在顶部（前 500 token）
- ❌ **有负面示例**: 缺失
- ✅ **spawn task 使用文件引用**: 符合（已注入执行契约）

**改进建议**:
- 添加正面示例 + 负面示例（Layer 2）
- 添加负面示例（通用检查）

---

### 5. research_module.md

#### Layer 1 检查（必须通过）✅

- ✅ **任务边界**: 有明确声明（"你是 Research 模块的编排器 Agent。你的工作：1. 直接通过 sessions_spawn 创建 Workers"）
- ✅ **状态转移表**: 有明确定义（"Wake Response Protocol" + "执行流程"）
- ✅ **完成条件**: 有明确声明（"全部步骤完成 + 写入 .research_completed 后才能结束 turn"）
- ✅ **恢复机制**: 有明确定义（"智能重试" + "失败报告格式"）
- ✅ **错误分类**: 有明确定义（瞬时故障 / 可恢复错误 / 不可恢复错误）
- ✅ **中间产物持久化**: 有明确定义（"stages/research_digest.json"）
- ✅ **没有 Fail Fast 思维**: ✅ 已移除 Fail Fast

#### Layer 3 检查（必须通过）✅

- ✅ **输入源**: 有明确定义（"planning_convergence"、"research_plan" 等）
- ✅ **输出目标**: 有明确定义（"stages/research_digest.json"）
- ✅ **中间产物清单**: 有明确定义（Worker Spawn 清单）

#### Layer 2 检查（推荐通过）⚠️

- ⚠️ **质量预期**: 部分定义（"验证 Worker 输出 — 确认每个 Worker 的输出已写入 Blackboard 并符合 Schema"）
- ✅ **验证标准**: 有明确定义（"信息守恒检查"）
- ❌ **正面示例 + 负面示例**: 缺失

#### 通用检查 ✅

- ✅ **Prompt 结构清晰**: 分层 + 编号
- ✅ **关键约束放在顶部**: 生存铁律在顶部（前 500 token）
- ❌ **有负面示例**: 缺失
- ✅ **spawn task 使用文件引用**: 符合（已注入执行契约）

**改进建议**:
- 添加正面示例 + 负面示例（Layer 2）
- 添加负面示例（通用检查）

---

## 总结

### 已修复的问题 ✅

1. ✅ **Fail Fast 思维已移除**: 5 个 prompt 都已移除 Fail Fast，改为智能恢复/智能重试
2. ✅ **契约笼子已添加**: 5 个 prompt 都已添加契约笼子（输入契约、输出契约、错误处理契约）
3. ✅ **错误分类已定义**: 5 个 prompt 都已定义错误分类（瞬时故障 / 可恢复错误 / 不可恢复错误）
4. ✅ **恢复机制已定义**: 5 个 prompt 都已定义恢复机制（智能恢复/智能重试）
5. ✅ **执行契约已注入**: planning_module.md 和 research_module.md 的 spawn task 已注入执行契约

### 待改进的问题 ⚠️

1. ⚠️ **正面示例 + 负面示例缺失**: 5 个 prompt 都缺少正面示例和负面示例（Layer 2 推荐检查）
2. ⚠️ **负面示例缺失**: 5 个 prompt 都缺少负面示例（通用检查）

### 下一步建议

**优先级 P1**（建议修复）:
- 为 5 个 prompt 添加正面示例 + 负面示例（Layer 2）
- 为 5 个 prompt 添加负面示例（通用检查）

**优先级 P2**（可选）:
- 继续检查 depth-3 的 Worker prompts（15 个）
- 为 depth-3 Worker prompts 添加契约笼子

---

## 附录：V2 检查清单

### Layer 1 检查（必须通过）

- [ ] 有明确的"任务边界"声明（负责/不负责）
- [ ] 有显式的"状态转移表"（每个状态有出边）
- [ ] 有"完成条件"声明（成功条件 + 无法恢复条件）
- [ ] 有"恢复机制"定义（重试策略、降级策略、替代方案）
- [ ] 有"错误分类"定义（瞬时故障/可恢复错误/不可恢复错误）
- [ ] 有"中间产物持久化"定义（文件路径或 blackboard 位置）
- [ ] **❌ 没有 Fail Fast 思维**（不是"遇到问题就退出"）

### Layer 3 检查（必须通过）

- [ ] 有明确的"输入源"定义（文件路径或变量名）
- [ ] 有明确的"输出目标"定义（文件路径或 blackboard 位置）
- [ ] 有"中间产物"清单（产物名 + 存放位置）

### Layer 2 检查（推荐通过）

- [ ] 有"质量预期"定义（量化指标或 golden example）
- [ ] 有"验证标准"定义（检查清单或验证代码）
- [ ] 有正面示例 + 负面示例

### 通用检查

- [ ] Prompt 结构清晰（分层 + 编号，不超过 3000 token）
- [ ] 关键约束放在顶部（前 500 token）
- [ ] 有负面示例（"以下行为违反约束"）
- [ ] spawn task 使用文件引用（不是内联 prompt）

---

**报告生成时间**: 2026-07-29  
**检查工具**: Prompt Doctor V2  
**检查人员**: 小满（AI Agent）
